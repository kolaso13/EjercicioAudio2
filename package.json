{
  "name": "dos",
  "version": "0.0.2",
  "description": "Exercise two",
  "repository": "https://github.com/kolaso13/EjercicioAudio2.git",
  "author": "kolaso13 <adrbrarare@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "parcel build templates/*.html",
    "prebuild": "(test -d dist || mkdir dist) && cp -rv media dist",
    "prestart": "yarn prebuild",
    "start": "parcel serve templates/*.html --open"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.0.0",
    "autoprefixer": "^10.3.7",
    "parcel": "^2.0.0",
    "posthtml-extend": "^0.6.0",
    "posthtml-include": "^1.7.2"
  }
}
